<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>映画一覧 (管理画面)</title>
</head>
<body>
  <h1>映画一覧 (管理画面)</h1>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>タイトル</th>
        <th>公開年</th>
        <th>概要</th>
        <th>画像</th>
        <th>上映状態</th>
        <th>登録日時</th>
        <th>更新日時</th>
      </tr>
    </thead>
    <tbody>
      <% @movies.each do |movie| %>
        <tr>
          <td><%= movie.id %></td>
          <td><%= movie.name %></td>
          <td><%= movie.year %></td>
          <td><%= movie.description %></td>
          <td><a href="<%= movie.image_url %>">画像</a></td>
          <td><%= movie.showing_status %></td>
          <td><%= movie.created_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
          <td><%= movie.updated_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
